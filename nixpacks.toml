[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["python310", "ffmpeg"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "python bot.py"
